apiVersion: v1
kind: Service
metadata:
  name: medusa-storefront
  namespace: medusa
spec:
  type: LoadBalancer
  selector:
    app: medusa-storefront
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
