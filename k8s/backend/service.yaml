apiVersion: v1
kind: Service
metadata:
  name: medusa-backend
  namespace: medusa
spec:
  type: LoadBalancer
  selector:
    app: medusa-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9000
